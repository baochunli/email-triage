[project]
name = "email-triage"
version = "0.1.0"
description = "Automation helpers for email triage and drafting."
requires-python = ">=3.10"
dependencies = [
  "PyYAML>=6.0.1",
]

[tool.uv]
package = false
